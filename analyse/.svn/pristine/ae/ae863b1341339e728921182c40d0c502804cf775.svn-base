;(function($){
	// 省略号
	$(".guide_list li .li_nr .nr").each(function(index, el) {
	 	var html = $(this).text();
		$(this).overTexts({
	        texts: html,
	        textLength:82,                  
	        ooType: "3"                  
		});
	});

	$(".hot_news .list .nr").each(function(index, el) {
	 	var html = $(this).text();
		$(this).overTexts({
	        texts: html,
	        textLength:22,                  
	        ooType: "3"                  
		});
	});
	// 分页
	$('#pageTool').Paging({
        pagesize:10,	//每页的列表数
        count:1000,		//一共的列表数
    });
})(jQuery);